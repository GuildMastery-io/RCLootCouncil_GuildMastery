# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# Build artifacts
*.zip
release/
build/

# OpenSpec workspace (local-only, not part of the addon distribution)
openspec/
.openspec/

# Node deps (the test/ harness is versioned, but not its dependencies)
node_modules/

# Local tooling state
.tokensave/
.claude/
